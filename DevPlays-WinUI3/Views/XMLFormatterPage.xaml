<Page
    x:Class="DevPlays_WinUI3.Views.XMLFormatterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animations="using:CommunityToolkit.WinUI.UI.Animations"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:models="using:DevPlays_WinUI3.Core.Models"
    mc:Ignorable="d"
    Style="{StaticResource PageStyle}">

    <Grid x:Name="ContentArea">
        <StackPanel Spacing="12" Orientation="Horizontal" >
            <StackPanel>
                <TextBlock Text="Plain XML" FontSize="24" />
                <TextBox x:Name="plainBox" Width="800" Height="200" Grid.Row="1" Grid.Column="0" TextChanged="XAML_Changed" TextWrapping="Wrap" AcceptsReturn="True" IsSpellCheckEnabled="True" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Formatted XML" FontSize="24" />
                <TextBox Width="800" x:Name="formattedBox" Height="200" Grid.Row="1" Grid.Column="0" IsReadOnly="True"  TextWrapping="Wrap" AcceptsReturn="True" IsSpellCheckEnabled="True"
        SelectionHighlightColor="Green"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Page>
